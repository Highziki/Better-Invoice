<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Business Logo</title>
    <link rel="stylesheet" href="./../../styles.css" />
  </head>
  <body class="bg-neutral-100">
    <%- include('./../partials/nav') %>

    <div class="form-container my-[7%] mx-auto h-[24rem]">
      <h1>Upload Business Logo</h1>

      <% if (error) { %>
      <small class="text-red-500"
        ><i class="fas fa-exclamation-circle"></i> <%= error%>
      </small>
      <% } %>

      <form
        action="/businesses/upload-business-logo/<%= business?.id %>?_method=PUT"
        method="post"
        enctype="multipart/form-data"
      >
        <input type="file" name="logo" class="home-input border-transparent" />

        <button type="submit" class="home-action-btn">
          <i class="fa-solid fa-upload"></i>
        </button>
      </form>
    </div>
  </body>
</html>
