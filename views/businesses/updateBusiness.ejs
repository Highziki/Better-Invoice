<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update Business</title>
    <link rel="stylesheet" href="./../styles.css" />
  </head>
  <body class="bg-neutral-100">
    <%- include('./../partials/nav') %>

    <div class="form-container my-[6%] mx-auto">
      <h1>Update Business</h1>

      <% if (error) { %>
      <small class="text-red-500"
        ><i class="fas fa-exclamation-circle"></i> <%= error%>
      </small>
      <% } %>

      <form
        action="/businesses/update-business/<%= business?._id %>?_method=PUT"
        method="post"
      >
        <label>Name:</label>
        <input
          type="text"
          name="name"
          id=""
          class="home-input border-black"
          value="<%= business?.name %>"
        />

        <label>Phone Number: </label>
        <input
          type="tel"
          name="phoneNumber"
          id=""
          value="<%= business?.phoneNumber %>"
          class="home-input border-black"
        />

        <label>Address:</label>
        <textarea
          name="address"
          id=""
          cols="30"
          rows="10"
          class="home-input border-black"
        >
        <%= business?.address %>
      </textarea
        >

        <label>Country</label>
        <input
          type="text"
          name="country"
          id=""
          value="<%= business?.country %>"
          class="home-input border-black"
        />

        <button type="submit" class="home-action-btn">Update</button>
      </form>
    </div>
  </body>
</html>
